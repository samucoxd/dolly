{% extends 'base.html.twig' %}

{% block title %}Reservaproducto{% endblock %}

{% block body %}
    <h1>Reservaproducto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservaproducto.id }}</td>
            </tr>
            <tr>
                <th>Fechaentrega</th>
                <td>{{ reservaproducto.fechaentrega ? reservaproducto.fechaentrega|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Obs</th>
                <td>{{ reservaproducto.obs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservaproducto_index') }}">back to list</a>

    <a href="{{ path('reservaproducto_edit', {'id': reservaproducto.id}) }}">edit</a>

    {{ include('reservaproducto/_delete_form.html.twig') }}
{% endblock %}
