{% extends 'base.html.twig' %}

{% block title %}Detalle Reserva{% endblock %}

{% block body %}
    <div class="row justify-content-md-center">
    <div class="col-6">
    <h1>Detalle Reserva</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservaproducto.id }}</td>
            </tr>
            <tr>
                <th>Fechaentrega</th>
                <td>{{ reservaproducto.fechaentrega ? reservaproducto.fechaentrega|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Obs</th>
                <td>{{ reservaproducto.obs }}</td>
            </tr>


    <a class="btn btn-info mr-2" href="{{ path('reservaproducto_index') }}">Volver a la Lista</a>

    <a class="btn btn-warning mr-2" href="{{ path('reservaproducto_edit', {'id': reservaproducto.id}) }}">Editar</a>

    {{ include('reservaproducto/_delete_form.html.twig') }}
            </tbody>
    </table>
</div>
</div>
{% endblock %}
