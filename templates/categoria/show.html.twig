{% extends 'base.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block body %}
<div class="row justify-content-md-center">
<div class="col-6">
    <h1>Categoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categorium.nombre }}</td>
            </tr>
        

    <a class="btn btn-info mr-2"  href="{{ path('categoria_index') }}">Ver categoria</a>

    <a class="btn btn-warning mr-2" href="{{ path('categoria_edit', {'id': categorium.id}) }}">editar</a>

    {{ include('categoria/_delete_form.html.twig') }}
    </tbody>
    </table>
    </div>
    </div>
{% endblock %}
